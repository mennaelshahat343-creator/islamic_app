<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- الإنترنت: لجلب القرآن، مواقيت الصلاة، والتلاوات الصوتية من الـ APIs -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- الموقع الجغرافي: لحساب مواقيت الصلاة الدقيقة واتجاه القبلة لمنطقة المستخدم -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- الاهتزاز: تأثير لمسي اختياري عند التسبيح -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- الإشعارات: تنبيهات الأذان والأذكار اليومية (مطلوبة صراحة من Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- جدولة تنبيهات دقيقة لمواعيد الصلاة حتى في وضع توفير الطاقة -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- تشغيل الصوت في الخلفية أثناء تلاوة القرآن -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="نور - تطبيق إسلامي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- يظهر أثناء تحميل محرك Flutter، ثم يُستبدل بشاشة البداية الفعلية للتطبيق -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- يتيح لـ Flutter تحديد أدوات مثل الأدوات المضمّنة (Deep Links) -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- استقبال إعادة تشغيل الجهاز لإعادة جدولة تنبيهات الصلاة -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

    </application>

    <!-- يسمح باستعلام حزم فتح الروابط الخارجية (مثل url_launcher) على Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
